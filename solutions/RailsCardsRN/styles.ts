import { StyleSheet } from "react-native";

export const appStyles = StyleSheet.create({
  card: {
    minWidth: 300,
    minHeight: 250,
    borderRadius: 10,
    borderWidth: 2,
    borderColor: "#777",
    alignItems: "center",
    justifyContent: "center",
    margin: 40,
    padding: 40,
    backgroundColor: "#fff"
  },
  labelCommand: {
    fontSize: 24
  },
  labelDefinition: {
    fontSize: 18
  }
});